# Implementation Plans

Generated by the improve skill on 2026-06-24 against commit `c027afb`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add critical bot/parser/request characterization tests | P1 | M | - | DONE |
| 002 | Restrict administrative bot commands to the owner | P1 | S | 001 | DONE |
| 003 | Make proxy changes affect the live Axios client | P1 | S | 001 | DONE |
| 004 | Serialize and await NIC parser runs | P1 | M | 001 | DONE |
| 005 | Update vulnerable dependencies without breaking runtime constraints | P1 | M | - | DONE |
| 006 | Preserve requested proxy when creating missing settings | P2 | S | 001 | DONE |
| 007 | Document setup and required environment variables | P2 | S | - | DONE |
| 008 | Spike configurable watched alert terms | P3 | M | 002, 006 | DONE |
| 009 | Spike owner-only operational status command | P3 | M | 002, 004 | DONE |
| 010 | Add fixture-based NIC parser regression tests | P2 | M | 001, 004 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- 001 comes before behavior fixes because the risky surfaces currently have no
  tests: bot commands, parser scheduling, request proxy behavior, and settings
  writes.
- 002 should land before direction plans that add more owner-only controls.
- 003 and 006 both touch proxy behavior; 006 is smaller but depends on the test
  harness from 001.
- 004 should land before 009 and 010 because both rely on a parser run having a
  well-defined lifecycle.
- 005 is independent, but must preserve `whois` pinned exactly to `2.14.2`.

## Real production validation note

The operator has confirmed that real database validation is available with:
`ssh root@drugoi.xyz`. Plans that use it mark it as optional and require
read-only MongoDB checks unless the operator explicitly approves a write. Do
not print secret values, `.env` contents, or connection strings in logs or plan
updates.

## Findings considered and rejected

- None. All audited findings and direction options were converted into plans.

## Reconciliation log

- 2026-06-25 (`d282d72`): Reconciled all DONE plans after merge to `master`.
  Verified current HEAD with `npm run test:run` (54 passed), `npm run lint`,
  `npm run build`, and `npm audit --audit-level=high`; all exited 0. Audit
  still reports residual low/moderate advisories only (1 low, 6 moderate), so
  plan 005 remains satisfied. Spot-checked merged feature symbols for owner-only
  commands, live Axios refresh, serialized parser execution, watch terms, status
  tracking, and parser fixture validation. No TODO, BLOCKED, or REJECTED plans
  remain.
